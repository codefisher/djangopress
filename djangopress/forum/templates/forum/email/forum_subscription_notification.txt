{{ user.username }},

{{ thread.author_name }} has just posted a new thread title "{{ thread.subject }}" in the forum "{{ thread.forum.name }}" that you are subscribed to.

The thread can be found here:
{{ scheme }}://{{ site.domain }}{{ thread.get_absolute_url }}

Regards,

The {{ forums.name }} team.
{{ scheme }}://{{ site.domain }}{{ forums.get_absolute_url }}
